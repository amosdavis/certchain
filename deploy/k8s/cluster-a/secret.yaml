apiVersion: v1
kind: Secret
metadata:
  name: certchain-avx
  namespace: certchain
type: Opaque
stringData:
  # Primary cluster polls AppViewX. Fill in real values before applying.
  AVX_URL: "https://avx.example.com"
  AVX_KEY: "<your-api-key>"
